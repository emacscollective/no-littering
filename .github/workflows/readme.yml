name: Readme
on:
  push:
    branches: main
jobs:
  manual:
    name: Readme
    uses: | # zizmor: ignore[unpinned-uses] same maintainer as this repo
      emacscollective/workflows/.github/workflows/readme.yml@main
    permissions: {}
    secrets:
      rclone_config: ${{ secrets.RCLONE_CONFIG }}
